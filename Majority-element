class Solution(object):
    def majorityElement(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        a=Counter(nums)

        for i, j in a.items():
                 if j > len(nums) // 2:
                    return i
            
